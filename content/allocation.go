package main

func main() {
	// allocation with new
	// No inicializa la memoria, solo la pone a cero
	//Asigna almacenamiento en cero
	//devuelve un puntero

	//Allocation with make
	/*
			crea slices, maps and channels only.
		Las tipos representan referencias a estructura de datos que deben inicializarse antes de su uso

	*/

	//var p *[]int = new([]int)      //asigna la estructura del segmento. *p=nil
	//var v []int = make([]int, 100) //el segmento v refiere a una nueva matriz de 100 ints

}
